import { Box, Heading, Text, VStack } from "@chakra-ui/react";

const About = () => {
  return (
    <VStack p={8}>
      <Heading mb="8">About Page</Heading>
      <Text>This is the about page for the Todo App.</Text>
    </VStack>
  );
};

export default About;
